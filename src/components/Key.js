import React from 'react';
/* eslint-disable  */

class Key extends React.Component {
    render() {
        return (
            <button>{this.props.value}</button>
        )
    }
}

export default Key;